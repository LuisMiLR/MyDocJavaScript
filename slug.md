

## Qu’est-ce qu’un *slug* en informatique ?

En informatique, un **slug** est une **chaîne de caractères** utilisée pour **identifier un élément** (comme un article, un produit ou une page).
Il est souvent employé dans les **URL** pour améliorer :

* le **référencement** (*SEO* — Search Engine Optimization)
* et l’**expérience utilisateur** (lisibilité et esthétique des liens).

---

### 💡 Exemple

Disons que vous avez un produit appelé :

> **"Easy assembly dining table"**

Ce nom ne peut pas être utilisé tel quel dans une URL, car il contient des **espaces**, ce qui le rendrait peu esthétique, par exemple :

```
https://example.com/item/Easy assembly dining table
```

---

### ✅ Solution : le slug

On transforme le nom en **slug** en remplaçant les espaces et caractères spéciaux par des tirets (`-`) :

```
easy-assembly-dining-table
```

Ainsi, l’URL devient :

```
https://example.com/item/easy-assembly-dining-table
```

---

Le slug permet donc d’avoir une **URL propre, lisible et optimisée**.
