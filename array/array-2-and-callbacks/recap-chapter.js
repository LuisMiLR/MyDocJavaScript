/* 
//*************************************************************************** Récapitulatif du chapitre *******************************************************************
//*************************************************************************************************************************************************************************

La .filter()méthode renvoie un nouveau tableau contenant certains éléments du tableau d'origine, en fonction de la condition que vous spécifiez.

JavaScript prendra votre fonction de rappel et l'appellera pour chaque élément du tableau.

Pour la .filter()méthode, le résultat de la fonction de rappel compte. Quand c'est le cas true, l'élément sera inclus dans le tableau résultant. Sinon, ce ne
 sera pas le cas.

JavaScript ne peut pas deviner intelligemment que le numberstableau devient le numberparamètre de votre fonction de rappel. Ce qu'il fait, c'est appeler 
votre fonction de rappel tout en donnant une valeur pour le premier paramètre que vous avez spécifié.

Utilisez la convention de dénomination pluriel -> singulier lorsque vous utilisez la .filter()méthode.

La .find()méthode renvoie le premier élément qui correspond à la condition que vous spécifiez. Si aucun élément n'était trouvé, vous récupéreriez undefined.

La .filter()méthode renvoie toujours un tableau même s'il est vide.

La .map(callback)méthode array permet de transformer un tableau en un autre.

La .includes(item)méthode tableau prend un itemet renvoie truelorsqu'il itemexiste dans le tableau et falseautrement.

La .join(glue)méthode array renvoie une chaîne d'éléments du tableau séparés par le glue.
*/
