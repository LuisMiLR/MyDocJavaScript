// numbers

/* Séparateur numérique ( _)
Vous pouvez représenter plus clairement des nombres plus grands, 
si vous le souhaitez, avec le séparateur numérique ( _). Par exemple: */

let nb = 1_000_000; // 1000000 (1 million)

//Converting from number to string
/* Bien que rarement utilisé, vous pouvez convertir un nombre en chaîne en appelant la .toString()méthode . */

let answer = 42;
answer.toString(); //"42"

/* Documentation sur le Web
Si vous jetez un œil à la documentation en ligne, vous verrez souvent String.prototype.toString(). Pourquoi y a-t-il un prototype? Ceci est abordé en profondeur plus loin dans ce cours. Pour l’instant, chaque fois que vous voyez String.prototype.something(), 
cela signifie qu’il existe une méthode something()que vous pouvez appeler sur un String . */
